[
  {
    "id": "offline-reload",
    "claim": "Works offline after setup",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "Fresh browser context loads /demo, goes offline, then marks a shared chore done."
  },
  {
    "id": "csv-export",
    "claim": "Export receipts as CSV",
    "where": "Receipt log and README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "Demo receipt log downloads a CSV whose header is chore,completed_at,due_at."
  },
  {
    "id": "local-only",
    "claim": "Nothing is sent to us",
    "where": "Privacy page and README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "Demo completion flow records all requests and accepts only the product's own origin."
  },
  {
    "id": "free",
    "claim": "Free to use",
    "where": "landing facts",
    "test": "npm test -- --grep @claim:free",
    "sandbox": "Fresh landing page contains no checkout, payment, or purchase control."
  }
]
